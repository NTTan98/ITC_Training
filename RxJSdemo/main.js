// Promise.timeout = function (miliseconds) {
//   return new Promise(function (resolve, reject) {
//     console.log("timeout");
//     setTimeout(() => resolve(), miliseconds);
//   });
// };
// const timeout = Promise.timeout(1000);

// Xay dung Observable co ban voi timeout va interval
function Observable() {}
